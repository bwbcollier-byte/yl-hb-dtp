name: Nightly Database Cleanup

on:
  schedule:
    - cron: "0 2 * * *" # 2am nightly
  workflow_dispatch: # Allow manual trigger

jobs:
  cleanup:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      # Note: This is a placeholder for running SQL via Supabase CLI or psql
      # You would need to set up SUPABASE_DB_URL secret
      - name: Run Cleanup Scripts
        run: |
          echo "Executing nightly database cleanup scripts..."
          # Example: psql $SUPABASE_DB_URL -f sql/cleanup/02-social-dedup.sql
          # Example: psql $SUPABASE_DB_URL -f sql/cleanup/05-backlink-repair.sql
          # Example: psql $SUPABASE_DB_URL -f sql/cleanup/07-social-rank.sql
